/*
 * vacuum.h — vacuum, spatium vacuum
 */

#ifndef VACUUM_H
#define VACUUM_H

typedef struct {
    int placeholder;        /* stub — expandetur */
} vacuum_t;

struct tabula;
void vacuum_initia(void);

#endif /* VACUUM_H */
